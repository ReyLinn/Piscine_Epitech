alias TimeManager.Repo
alias TimeManager.Time.WorkingTime

Repo.insert! %WorkingTime{
  startTime: DateTime.truncate(DateTime.utc_now(), :second),
  endTime: DateTime.truncate(DateTime.utc_now(), :second),
  user_id: 1
}

Repo.insert! %WorkingTime{
  startTime: DateTime.truncate(DateTime.utc_now(), :second),
  endTime: DateTime.truncate(DateTime.utc_now(), :second),
  user_id: 1
}

Repo.insert! %WorkingTime{
  startTime: DateTime.truncate(DateTime.utc_now(), :second),
  endTime: DateTime.truncate(DateTime.utc_now(), :second),
  user_id: 1
}

Repo.insert! %WorkingTime{
  startTime: DateTime.truncate(DateTime.utc_now(), :second),
  endTime: DateTime.truncate(DateTime.utc_now(), :second),
  user_id: 2
}

Repo.insert! %WorkingTime{
  startTime: DateTime.truncate(DateTime.utc_now(), :second),
  endTime: DateTime.truncate(DateTime.utc_now(), :second),
  user_id: 2
}

Repo.insert! %WorkingTime{
  startTime: DateTime.truncate(DateTime.utc_now(), :second),
  endTime: DateTime.truncate(DateTime.utc_now(), :second),
  user_id: 3
}

Repo.insert! %WorkingTime{
  startTime: DateTime.truncate(DateTime.utc_now(), :second),
  endTime: DateTime.truncate(DateTime.utc_now(), :second),
  user_id: 3
}

Repo.insert! %WorkingTime{
  startTime: DateTime.truncate(DateTime.utc_now(), :second),
  endTime: DateTime.truncate(DateTime.utc_now(), :second),
  user_id: 3
}

Repo.insert! %WorkingTime{
  startTime: DateTime.truncate(DateTime.utc_now(), :second),
  endTime: DateTime.truncate(DateTime.utc_now(), :second),
  user_id: 4
}

Repo.insert! %WorkingTime{
  startTime: DateTime.truncate(DateTime.utc_now(), :second),
  endTime: DateTime.truncate(DateTime.utc_now(), :second),
  user_id: 4
}

Repo.insert! %WorkingTime{
  startTime: DateTime.truncate(DateTime.utc_now(), :second),
  endTime: DateTime.truncate(DateTime.utc_now(), :second),
  user_id: 5
}

Repo.insert! %WorkingTime{
  startTime: DateTime.truncate(DateTime.utc_now(), :second),
  endTime: DateTime.truncate(DateTime.utc_now(), :second),
  user_id: 5
}

Repo.insert! %WorkingTime{
  startTime: DateTime.truncate(DateTime.utc_now(), :second),
  endTime: DateTime.truncate(DateTime.utc_now(), :second),
  user_id: 5
}

Repo.insert! %WorkingTime{
  startTime: DateTime.truncate(DateTime.utc_now(), :second),
  endTime: DateTime.truncate(DateTime.utc_now(), :second),
  user_id: 6
}

Repo.insert! %WorkingTime{
  startTime: DateTime.truncate(DateTime.utc_now(), :second),
  endTime: DateTime.truncate(DateTime.utc_now(), :second),
  user_id: 6
}

Repo.insert! %WorkingTime{
  startTime: DateTime.truncate(DateTime.utc_now(), :second),
  endTime: DateTime.truncate(DateTime.utc_now(), :second),
  user_id: 6
}

Repo.insert! %WorkingTime{
  startTime: DateTime.truncate(DateTime.utc_now(), :second),
  endTime: DateTime.truncate(DateTime.utc_now(), :second),
  user_id: 7
}

Repo.insert! %WorkingTime{
  startTime: DateTime.truncate(DateTime.utc_now(), :second),
  endTime: DateTime.truncate(DateTime.utc_now(), :second),
  user_id: 7
}
